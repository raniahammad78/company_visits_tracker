<odoo>
    <record id="view_visit_contract_form" model="ir.ui.view">
        <field name="name">visit.contract.form</field>
        <field name="model">visit.contract</field>
        <field name="arch" type="xml">
            <form string="Visit Contract">
                <header>
                    <button name="action_start_contract" string="Start Contract" type="object" class="btn-primary" invisible="state != 'draft'"/>

                    <button name="action_open_extra_visit_wizard" string="Add Extra Visits" type="object" class="btn-secondary" invisible="state != 'in_progress'"/>

                    <button name="action_generate_current_month_visits" string="Generate Current Month's Visits" type="object" class="btn-secondary" invisible="state != 'in_progress'"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,in_progress,done"/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                         <button name="action_open_visits" type="object" class="oe_stat_button" icon="fa-tasks">
                            <field name="visits_count" widget="statinfo" string="Generated Visits"/>
                        </button>
                    </div>
                    <div class="oe_title">
                        <h1><field name="name"/></h1>
                    </div>
                    <group>
                        <group>
                            <field name="partner_id"/>
                            <field name="visits_per_month"/>
                             <field name="total_contract_visits" readonly="1"/>
                        </group>
                        <group>
                            <field name="start_date"/>
                            <field name="end_date"/>
                        </group>
                    </group>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_visit_contract_list" model="ir.ui.view">
        <field name="name">visit.contract.list</field>
        <field name="model">visit.contract</field>
        <field name="arch" type="xml">
            <list string="Visit Contracts">
                <field name="name"/>
                <field name="partner_id"/>
                <field name="start_date"/>
                <field name="end_date"/>
                <field name="visits_per_month"/>
                <field name="state"/>
            </list>
        </field>
    </record>

    <record id="action_visit_contract" model="ir.actions.act_window">
        <field name="name">Contracts</field>
        <field name="res_model">visit.contract</field>
        <field name="view_mode">list,form,kanban</field>
    </record>
</odoo>